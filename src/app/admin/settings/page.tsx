"use client"

import AdminHeader from "@/components/AdminHeader"

export default function AdminSettings() {
	return (
		<AdminHeader header={<h1 className="text-xl font-semibold">Settings</h1>}>
			<div>Settings</div>
		</AdminHeader>
	)
}
